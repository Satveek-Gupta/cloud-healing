kind: Service
apiVersion: v1
metadata:
  name: backend-service
  namespace: cloud-healing
spec:
  selector: 
    apps: backend
  ports:
    - protocol: "TCP"
      port: 5000
      targetPort: 5000
  type: NodePort
